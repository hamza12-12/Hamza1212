<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>بوت أب يزن</title>
  <style>
    :root { --neon:#00c3ff; --pink:#ff00ff; }
    body {
      margin:0; padding:0; height:100vh;
      background: radial-gradient(circle at 20% 0%, #121221, #0b0b14 60%, #000);
      color:#fff; font-family: Arial, sans-serif;
      display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center; overflow:hidden;
    }
    h1 { font-size:54px; color:var(--pink);
      text-shadow:0 0 10px var(--pink),0 0 20px #ff33ff,0 0 40px var(--pink); margin:0 0 12px;
    }
    p { font-size:18px; opacity:.9; margin:10px 0 24px; }
    .btn { padding:14px 32px; background:var(--neon); color:#000; font-weight:bold;
      border-radius:12px; text-decoration:none; font-size:20px;
      box-shadow:0 0 20px var(--neon),0 0 40px var(--neon); transition:.25s;
    }
    .btn:hover { transform:translateY(-2px); box-shadow:0 0 28px var(--neon),0 0 60px var(--neon); }
    .robot { font-size:88px; margin:16px 0; animation:float 3s ease-in-out infinite;
      text-shadow:0 0 14px var(--neon),0 0 28px var(--neon);
    }
    @keyframes float { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-12px)} }
    /* نجوم متحركة خفيفة */
    .stars, .stars2 {
      position:fixed; inset:0; pointer-events:none; background-repeat:repeat;
      background-image: radial-gradient(2px 2px at 40px 60px, rgba(255,255,255,.8), rgba(255,255,255,0) 40px),
                        radial-gradient(1.5px 1.5px at 120px 200px, rgba(255,255,255,.6), rgba(255,255,255,0) 30px);
      opacity:.25;
    }
    .stars { animation: drift 25s linear infinite; }
    .stars2 { animation: drift 45s linear infinite; opacity:.18; }
    @keyframes drift { from { background-position:0 0 } to { background-position:300px 600px } }
    /* خطوط نيون متقاطعة */
    .beams::before, .beams::after {
      content:""; position:fixed; left:0; right:0; top:0; bottom:0; pointer-events:none;
      background: linear-gradient(115deg, transparent 46%, rgba(0,195,255,.12) 50%, transparent 54%),
                  linear-gradient(245deg, transparent 46%, rgba(255,0,255,.08) 50%, transparent 54%);
      mix-blend-mode: screen;
    }
  </style>
</head>
<body>
  <div class="stars"></div>
  <div class="stars2"></div>
  <div class="beams"></div>

  <h1>بوت أب يزن 🚀</h1>
  <div class="robot">🤖</div>
  <p>مرحباً! اضغط الزر لبدء التواصل على تيليجرام</p>

  <!-- غيّر اسم البوت هنا -->
  <a href="https://t.me/اسم_البوت" class="btn">ابدأ الآن</a>
</body>

</html>
